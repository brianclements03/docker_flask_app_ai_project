version: '3.8'

services:
  chromadb:
    image: chromadb/chroma
    container_name: chromadb
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/chroma

volumes:
  chroma_data:
